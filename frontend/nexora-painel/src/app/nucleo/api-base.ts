import { environment } from '../../environments/environment';

/** Endereços da API. Este arquivo só LÊ do environment — nenhuma URL é escrita aqui.
 *
 *  Trocar de ambiente é trocar o arquivo de environment, não editar código. */
export const API = environment.apiBase;
export const HUB = environment.hubBase;

meta {
  name: PUT /conta
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/conta
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nome": "",
    "email": "{{email}}"
  }
}
